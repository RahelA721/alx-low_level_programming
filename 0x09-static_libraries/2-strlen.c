#include "main.h"

/**
 * _strlen - returns length of string
 * @s: the string
 *
 * Return: the string length
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
